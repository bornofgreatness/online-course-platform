# TODO
- [x] Fix `app/admin/page.tsx` control-flow/return structure so it compiles as a Server Component.
- [x] Ensure Prisma queries only run in the admin-authorized branch.
- [x] Ensure the returned JSX is the only return from the component (no stray JSX).
- [ ] Re-run `tsc` / build/lint (as possible in this environment) to confirm no syntax errors.
